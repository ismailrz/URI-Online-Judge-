#include<bits/stdc++.h>
using namespace std;
int main()
{
    string A,B;
    int n,i;
    getline(cin,A);
    scanf("%d%*c",&n);
    getline(cin,B);
    for(i=0;i<B.size();i++){
            if(B[i]=='<'){
                if(B[i]==A[p++])
                    c++;
                if(c>=A.size()){


                }
            }

    }

    return 0;
}

